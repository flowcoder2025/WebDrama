import { describe, it, expect } from "vitest";

describe("프로젝트 초기화", () => {
  it("TypeScript 환경이 정상 동작한다", () => {
    const value: string = "WebDrama";
    expect(value).toBe("WebDrama");
  });
});
